# Anexar tuplas a listas
numeros = [1, 2, 3, 4, 5]  # Esto es una lista
numeros += (6, 7)  # Los elementos entre los parentesis son una tupla
print(numeros)  # se anexaron los elementos de la tupla a la lista
